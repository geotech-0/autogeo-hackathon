# 실자료 화면·동선 검수

2026-09-21. 기능 담당자의 Chrome 자체검수와 root의 Codex IAB 독립검수를 구분했다. 실자료 버전의 캡처는 인수 폴더 `qa/real-site`, `qa/real-field`, `qa/real-design`에 보관한다. 이전 합성판 캡처를 이번 완료 근거로 사용하지 않는다.

| 화면 | 확인한 내용 |
|---|---|
| 개요 | 실제 정사영상과 현장명,4차수32공/3개월자료,빈검토이력 시작동선 |
| 실제 지도 | 차수색·32공선택·상세층서·CAD경계·확대·이동·주석,390px 표/좌표 가독성 |
| 실제3D/단면 | DSM/점군/경계면 구분,모델전체보기,관찰종료와미확인경계,축/단위·모바일선택공 |
| 설계 | 위치/앵커단 선택,허용변수만편집,원문대비값·수식·Jf연결,수동값근거와개정복원 |
| 계측·품질 | 센서목록·기간·원시표,실계측/다른현장예제,판정보류·단위·원문출처 |
| 유지관리 | 빈목록·필수입력·첨부·조치일자/담당자/근거·마감/재개,모달키보드초점 |
| 자료함·기준 | 원문/출처 검색·없음 상태,PDF쪽번호·확대·원문텍스트,채택판과기준참고 구분 |
| 공개후보 | 원문이미지를 요청하지 않고 페이지출처/층서 유지,제외한PDF링크는안내표시 |

## 이번 검수에서 수정한 문제

- 원문 PDF iframe이 IAB에서 빈 화면으로 표시돼 PDF.js canvas와 텍스트보기·쪽이동·확대로 교체했다.
- 설계 record URL 새로고침에서 입력은 보존되지만 선택 저장ID가 풀리는 문제를 수정했다. 정확한record를 복원하고 개정저장으로 이어진다.
- 모바일3D 전체보기의 화면비와 여백을 보완하고 확대/회전/페이지스크롤 조작을 설명했다.
- 계측F2의 단위 미확인 비율은 ‘원문 기준 수치 대비·참고/단위 확인 전’으로 표시한다.
- DCPT 문헌환산표의 접근성 명칭을 ‘측정값’ 대신 ‘데이터점’으로 수정했다.
- 첨부모달의 Tab/Shift+Tab 초점가두기·Escape·원버튼복귀를 추가하고 실제 키보드로 확인했다.

1440px,1024px,390px의 대표 화면을 실제 viewport로 점검했다. 390px 지반지도·단면과 필드차트는 가로폭에 맞춰 표시되고 공학표는 자체 스크롤을 사용한다. 1024px 설계의 documentWidth는1024였다. 숫자·단위를 숨기는 강제축소로 대응하지 않는다.

모바일에서는 3D영역 밖에서 페이지를 스크롤한다. 3D영역의 휠/두손가락은 확대에 사용하며 전체보기로 복원할 수 있다. 공학 원문과 고밀도표는 확대/스크롤로 자세히 읽도록 구성했다.

검수는 에이전트가 수행했고 실제 사용자 만족도 조사·접근성 인증은 아니다. 아직 새 버전의 Production 배포 검수를 끝냈다고 주장하지 않는다.

## 배포 직전 보완 화면

첫 화면의 건수는 ‘저장한 검토의 후속 확인’임을 밝히고, 유지관리 카드는 ‘GPR 조사 기록’으로 표시한다. 시공 이슈의 모바일 해결 확인 체크박스와 버튼을 390px에서 직접 조작했다. 체크박스는 문구와 나란히 표시하고 후속 조치·추가 재점검·명시 마감 버튼은 세로로 배치해 글자가 쪼개지지 않도록 수정했다.

## 채운 지층 메쉬 후속 검수

root가 실제 IAB에서 불투명/반투명 메쉬, 회전, 층 숨김, 단면 열기, 경계면 전환과 색이 채워진 동서 단면을 확인했다. 저장→경계면으로 변경→저장안 불러오기→새로고침에서 채운 지층·불투명도·단면 열림 상태를 복원했다. 390px에서는 제어부가 줄바꿈되고 documentWidth=390이었다. QA 지반 기록 1개는 로컬 검수 브라우저에만 저장했으며 배포 초기값에 포함하지 않는다.

## XYZ 지층 탐색기

root CUA 실조작으로 전체 3층의 매끈한 외곽, Z=55m 수평 절단과 자유 회전·상부 시점, X=239930m와 Y=521620m 단면 및 정면/단면만 보기를 확인했다. 표시 하한 30→20m에서 암반 표시 두께가 갱신됐다. 잘못된 Y=1 입력 중 저장과 JSON 내보내기가 비활성화됐고 중앙 복구 후 다시 활성화됐다. 표시 하한20/Z55/보기 상태를 r2에 저장하고 다른 보기·자동 하한으로 바꾼 뒤 저장안 복원·초안 저장 후 새로고침을 확인했다. 초안 자동저장은150ms 지연되며 명시 저장안은 검토 이력에서 복원한다.

데스크톱은 모델과 입력부를 나란히 배치해 슬라이더와 모델을 동시에 보도록 했고, 390px에서는 세로 배치와 documentWidth=390을 확인했다. 화면 DPR 변경 시 캔버스 픽셀비를 갱신한다. Z 평면도는 E/N 동일 축척이고, 수직 단면의 축에는 표고와 거리 단위를 표시한다. 현재 QA 이력은 로컬에만 존재한다.

## 작업 중심 화면 정리

- 지반: 중복 통계 카드를 한 줄로 통합하고, 3D·절단 조작을 앞에 배치했다. 표시 옵션·자료 한계·보간 설정은 native details로 펼치며, 암반 표시 하한·개략 정합·모델 오류는 기본 화면에 남는다. 모바일에서는 모델을 먼저 보이고 가로 넘침 없이 제어 패널을 이어 배치한다.
- 설계: 기본 자료 입력 9개를 4개로 줄이고 내부 출처 ID·판본을 상세로 옮겼다. 결과·판정이 원문 비교보다 먼저 보인다. 필수 출처/상세계수 오류는 자동으로 펼쳐진다.
- 시공: 계측 차트와 확인·조치를 먼저 표시하고 전체 원시표·설명은 펼침으로 제공한다. 모바일 계측기 선택창으로 긴 목록을 대체한다. F2 단위/요약 불일치와 품질시험 적용 보류 사유는 계속 노출한다.
- 유지관리/이력: 결과와 최근 조치를 먼저 표시하고 전체 이력·등록 정보·해시·내부 식별자를 상세로 보존한다.
- root는 데스크톱 1280px과 모바일 390px에서 모델, 설정 펼침, 암반 레이어 전환, Z55 절단, 설계 결과, F2 확인사항, 모바일 W-2 선택, 유지관리 r5 상세를 실제 브라우저로 확인했다. 모바일 지반의 document/scroll width는 모두 390px이었다.
