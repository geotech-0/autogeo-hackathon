# 실자료 버전 검증 기록

검증일: 2026-09-21 KST. 현재 작업본은 이천자이더리체 실자료 버전이다. 이전 합성판의 74개 시험과 Production 검수를 이번 버전의 증거로 재사용하지 않는다. 실제 공개 배포 완료는 아직 주장하지 않는다.

## 자동시험과 빌드

이번 통합 실행은 `npm test` 122/122 통과, 실패0이다. `node scripts/build-derived.mjs`는 TypeScript 검사와 Vite 빌드를 수행한 뒤 공개 출력에서 원문 폴더3개를 제거하고 실제 공간 자산 보존을 확인했다. 전체 로그는 별도 인수 패키지의 실자료 QA에 보관한다.

| 검증 대상 | 실제 검증 |
|---|---|
| 실제 설계 | 기존23개와 실제10개 시험. B좌/C/D의20부재 사례, 허용입력, 단위변환, Jf 전체정밀도 연결, 직접Qu, D원문모순, 보고서 출처·공개링크분리 |
| 실제 지반 | 지반20개 시험. 원문32공/96구간, 미확인경계, 관측점재현·외삽·중복/역전·특이행렬, 좌표변환/역변환, 저장내용검증 |
| 시공·유지관리 | 기존 검토 엔진과 실제자료/이력 추가시험. F2독립산술, 원시페이지, W단위/매핑, INC전사오류, L일련번호연결, 엄격한달력·CSV·재하순서, 보류·조치단계 |
| 공통 저장 | IndexedDB transaction, 동시개정, 과거개정, 충돌 시 전체원자성, 타현장거부, 초안hydration/늦은응답보호 |
| 첨부 | 바이트/hash 포함 export/import, 첨부변조·누락참조전체거부, 기존합성현장이력을 실제현장으로 바꿔넣지 못함 |
| 이력 재진입 | 요청한 record ID가 hydration 후 한 번만 복원되고 후속입력이 다른 기록 갱신으로 덮이지 않음 |

`react-test-renderer`의 upstream 폐기예정 안내는 시험에서 출력되지만 시험 실패는 없다. 서버·상용 해석기·외부AI API를 호출한 검증은 아니다.

## 독립 수치 대조

- 지반의 4차수와 전체32공 모델을 별도 NumPy 선형해와 대조했다. 845위치/2,535표고의 최대차5.97e-13m로 허용1e-7m 이내다. 이것은 구현 일치 검증이며 현장 지층 정확도의 증명이 아니다.
- TFW 픽셀중심 좌표를 모서리 기준으로 옮긴 affine과 GeoTIFF affine의 최대차는1.17e-10 이하다. CAD 내부 일관성 잔차를 독립 측량정확도로 사용하지 않는다.
- 원문 B좌1단은 R′45.620kN/m, 간격1.8m로 Treq82.116kN, Jf125.864272kN을 계산한다. 원문 Jf125.865와 인쇄 정밀도 차이를 보존한다.
- root가 UI에서 R′60을 입력해 Treq108kN, Jf151.748272kN을 확인했다. 연결띠장은 35°와 c/a=393/550를 적용해 R88.8215kN, M14.5344kN·m, V48.4481kN으로 갱신됐다. 이 입력은 QA 전용으로 명시했다.
- F2 원문 유량 `(2068.45−1072.67)/5=199.156m³/day`를 독립 산술로 대조했다. 요약표 불일치와 관리기준 단위 차이는 판정보류로 남는다.

## 실제 브라우저 조작

작성자와 다른 담당이 실제 CUA 클릭·키보드·파일선택으로 검수했다. 현업 사용자 연구나 시간절감 실험으로 표현하지 않는다.

- 실제 정사영상/시추공/CAD 지도, 지층모델·단면·DSM·LAS 전환, 원문PDF와 주상도 페이지 전환.
- 지반 검토 저장 후 다른공·다른설정으로 초안을 바꾸고 저장안을 불러와 선택공/차수/정합/보기상태 복원. 통합이력의 해당record 재진입.
- 실제 설계 입력변경→연결계산→저장→다른단면→이력재진입→새로고침→‘개정저장’ 및 r2보존. 초기 검수에서 저장ID 미복원 문제를 발견해 수정 후 다시 확인했다.
- 실제 F2 상세값199.156/136.539와 출처·보류 확인, 이슈 r1→조치r2, 품질4종의 축·단위·예제표시 확인, 실제지도 주석 저장.
- GPR 날짜/담당자/근거 누락 차단, 원본TXT 첨부→확인→조치→재점검→별도마감 r5. 새로고침, 첨부 원문 열람, SHA-256 대조.
- 첨부 포함 JSON 내보내기→해당 로컬 QA기록 초기화→실제파일선택 가져오기. 1기록/5개정/첨부1개와 본문 복원. 관련업무가 같은record를 연다.
- 첨부모달의 초점 진입, Shift+Tab/Tab 경계순환, Escape 닫기와 원래 첨부버튼 초점복귀를 root IAB에서 확인했다.
- PDF.js가 원문을 직접 렌더한다. 초기 native iframe의 빈 화면을 발견하여 교체했다. 실제 계측 PDF81쪽 수치를 눈으로 대조했다.

## 공개 배포 후보

실제 수치·정밀좌표·영상·공간형상은 포함하고, 원문 PDF9개/주상도이미지47개/품질원본7파일은 공개출력에서 제외하는 모드를 구현했다. 제외 안내는 원문 페이지 참조를 보존한다. 사용자 자신의 브라우저 첨부(blob)는 계속 열람할 수 있다.

현재 외부에 공개된 Vercel은 이전 합성판이다. 실제 파생자료의 GitHub/Vercel 외부 공개 승인을 받은 뒤에만 배포하고 최종SHA·공개HTTP·브라우저 검증을 릴리스 기록에 추가한다. 로컬 검수를 공개 배포 검수로 표시하지 않는다.

## 자료 자체의 미확정 범위

NH-4 총심도/층서, D깊이, 일부 센서 기준/위치와 유량단위·요약값, 인장계기단위는 원문확인이 더 필요하다. 실제GPR와 두번째드론회차는 제공되지 않았다. 흙막이 전체 자동해석·완전BIM·영상변형량·GPR원신호판독은 구현 완료로 주장하지 않는다.

## 배포 직전 후속 검수

기존 106개 시험 뒤 별도 코드 검토에서 시공 이슈 자동 마감, 계측 대표 출처 고정, DCPT 계산 입력 미저장의 세 결함을 발견했다. 이를 수정하고 실제 컴포넌트 기반 회귀 5개를 추가했다.

- 재점검은 열린 상태를 유지한다. 담당자·수행일·근거와 해결 확인을 갖춘 별도 마감만 closed로 바꾸며, 후속 조치와 재열기는 기존 이력을 보존한다. 새 UI에서 미해결 r3→후속 조치→명시 마감 r6→재열기→모바일 명시 마감 r9를 root가 확인했다.
- 계측 출처는 선택한 sourceRows의 실제 보고서·판본·페이지 집합으로 저장하고 선택 측정기간은 별도로 기록한다. W-2 1월은 monitoring-01 + monitoring-02, 문서판 2024-01, 2024-02이다. 상단 원문 링크도 선택 행을 따른다. F2 1월처럼 빈 기간은 저장을 거부한다.
- DCPT의 입력 2.4cm/타, N_DCPT 12.5, N_SPT 15.75, qa 196.875kPa를 저장한다. root가 다른 1.2 초안으로 변경한 뒤 이력 재진입에서 2.4와 결과 복원을 확인했다. 빈값·NaN·overflow를 거부하고 복원 후 사용자가 바꾼 초안을 다른 기록 갱신으로 덮지 않는 회귀를 통과했다.

이 결과는 기존 시험의 검증 범위를 소급해 확대하지 않는다. 새 전체 시험 111개와 공개 파생 빌드 결과가 후속 후보의 기준이다.

## 채운 지층 메쉬 검증

닫힌 상·하·측면과 절단 cap, 공유 모서리, 양의 체적, 독립 표면적분 체적 대조, 결측·외삽·역전·두께 0 제외를 검증했다. 실제 4개 차수와 전체 32공 모델에서도 열린 모서리 없이 연결됨을 확인했다. 미확인 암반 하단은 생성하지 않는다. 새 표시 설정의 과거 기록 기본값·JSON 왕복·오류 입력 거부를 추가해 전체 122개가 통과했다. 메쉬 체적 검산값은 검증된 토공량으로 UI에 제공하지 않는다.
