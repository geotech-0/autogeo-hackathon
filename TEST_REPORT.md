# AutoGeo 검증 기록

검증일: 2026-09-21 KST. 실제 실행 결과를 기록하며 자동시험, 에이전트 브라우저 검수, 실제 현장 원문 검산을 구분합니다. 최종 배포 식별값은 공개 `/version.json` 및 제출 패키지의 배포 기록으로 대조합니다.

## 계산·저장 자동시험

최종 통합 실행은 **74/74 통과, 실패0**입니다. `npm test`와 `npm run build`를 실제 실행했습니다. 공개 저장소의 기능 완성 commit `46c852ab58feb69682214cc570cf9552b191ef0c`를 인증 없이 새로 복제해 `npm ci` → 74/74시험 → 동일 SHA 빌드도 재현했습니다. Node25.9.0/npm11.12.1, npm audit0. 이후 단면 축 단위 겹침만 수정했으며 최종 개정의 재검증 결과는 제출 패키지에 함께 보존합니다.

| 대상 | 실제 결과와 검증 범위 |
|---|---|
| 원래 부재 검토 코드 | 이번 실행에서 관련 110/110, 전체 137/137 통과. 원래 프로젝트는 build script가 없어 build 시도는 실패로 기록 |
| 로컬 구조계산서 선정 사례 | 20/20: 앵커7·띠장7·H-Pile3·토류판3. 공통 수치 최대차0; 원문 반올림 차이는 별도 보존 |
| 공개 설계 | 정상·초과·필수값 누락, 독립 손계산, 단위폭→1개소 변환 한 번, 부호 원값 보존, 각 최댓값의 지배단계 구분, Qu/Fs, 편집 허용목록, 외부 해석 재확인, 보고서 escaping |
| 지반 | 15개 시험. 독립 폐쇄식·관측공 재현·LOO 누출 방지·3D와 단면 일치·중복/층 역전/단위/특이행렬·변환과 역변환·저장 복원 |
| 시공·유지관리 | 20개 시험. CSV 파싱/결측/중복/역전·단위/면적·달력·기준 미설정·이슈 상태·GPR 순서/보관/복원 |
| 공통 저장 | 가짜 IndexedDB를 통한 실제 transaction: 동시 개정 증가, 과거 개정 보존, import 전체 원자성, 충돌·타현장·비유한 수 거부, 분리된 복사본 |

검산 기대값은 엔진 결과에서 역생성하지 않았습니다. 지반 관측점 허용오차는 nugget=0에서 1e-7m/m², 실제 데이터의 NumPy 대조는 1e-8입니다. 설계는 전체 정밀도로 계산하고 표시 단계에서만 반올림합니다. 강성 영향 입력 정책을 강화한 중간 실행에서는 과거 기대값 1건이 실패했고 기대 상태를 재확인 필요로 수정한 뒤 통과했습니다. 실패 출력도 제출 패키지에 보존합니다.

## 수치 대조

| 입력 / 방법 | 실제 결과 |
|---|---|
| 앵커 R′40kN/m, 간격1.5m | Treq60kN → 손실 포함 Jf105.396299826kN |
| R′50 / 80kN/m | Jf120.3963 / 165.3963kN; 연결 띠장 M9.479 / 13.022kN·m |
| H-Pile Qu1400kN / Fs2 | Qa700kN |
| 원형 판 직경300mm, Q7.0685834705770345kN | A0.07068583470577035m², q100kPa |
| 합성 12공 ordinary kriging | 격자693, 알려진 식 RMSE0.104770m, LOO RMSE0.317809m, 교차0 |
| 합성 참고형상 대응점 맞춤 | 이동(6,−4)m, 회전7°, 축척0.96; 적합점 RMSE0, 별도 검사점0.364m |

## 실제 브라우저 확인

실제 사용자 시험이 아닌 **에이전트 브라우저 검수**입니다. 작성자 자체 확인과 비작성자 독립 확인을 수행했습니다. 공통 담당은 Codex IAB, 기능 담당은 별도 Chrome 탭을 사용했습니다. 초기 설계 smoke는 설치된 Chrome을 통한 자동화였으며 후속 공통 동선은 CUA로 확인했습니다.

- 현장 선택 → 시추 3D/단면 → 수동 반력 수정 → 띠장 연결값/계산 근거 → 저장 → 새로고침 복원을 확인했습니다.
- 전역 JSON 5기록/5개정 내보내기 → 초기화 취소 시 보존 → 확인 초기화 → 실제 파일 선택으로 가져오기 → 지반 입력 복원. 변환6/−4/7°/0.96과 검사점0.364m가 유지됐습니다.
- 실제 평판재하 CSV 파일 4행 업로드 → 출처 확인 전 저장 차단 → 합성 출처 선택 → q100kPa/최대침하1.2mm → 저장 → 새로고침 복원을 확인했습니다.
- 차트와 원시 표의 대표3점, 정상/초과/오류/기준 미설정 상태, 계측23.6mm/20mm 초과와 구역 이슈, 합성 영상0/50/100% 비교를 확인했습니다.
- GPR 필수 근거 누락 차단 → 조사/확인/조치/재점검 → 명시적 마감 → 새로고침/보관/복원을 확인했습니다.
- 기준 검색 일치/없음, 본문 조항 미확인 시 판정 보류 저장을 확인했습니다. 공식 기준 메타데이터는 합성 자료와 구분합니다.
- 1440·1024·390px에서 주요 화면과 조작을 확인했습니다. 상세 관찰·수정은 `VISUAL_QA.md`에 있습니다.
- 네트워크503을 로컬 테스트 프록시에서 주입한 뒤 복구 버튼으로 재요청/정상복귀 확인. WebGL 생성 실패도 테스트 프록시에서 주입해 오류 안내·단면 변경·시추표 사용을 확인했습니다. 실제 GPU 미지원 장치를 사용한 시험은 아닙니다.
- 초안 읽기 실패 시 편집·재시도 중에도 기존 데이터를 덮어쓰지 않는지 실제 React hook과 IndexedDB 실패 주입으로 검증했습니다. 읽기 복구·쓰기도 성공하면 오류가 해제되며 이전 쓰기의 늦은 응답이 최신 상태를 오염시키지 않습니다.

## 공개 배포와 산출물

- 기능 완성 개정을 Vercel Preview에서 먼저 확인한 뒤 main을 Production에 배포했습니다. `https://autogeo-hackathon.vercel.app`은 로그인 없이 열립니다. GitHub도 인증 설정 없이 clean clone했습니다.
- 인증 없는 HTTP 요청으로 개요/입찰/설계/시공/유지관리/이력/기준/버전 8경로와 정적 자산11개를 확인했습니다. HTML meta와 version.json의 commit을 대조했습니다. 런타임 localhost 의존은 없습니다.
- Production에서 지반 결과, 평판12.4mm 초과 이슈, 계측23.6mm 초과 이슈, GPR 조사→확인→조치(개정3), 설계R50을 저장했습니다. 새로고침 복원, 공식 기준의 보류/공식 참고자료 표시도 확인했습니다.
- Production에서 실제 HTML/입력JSON을 내려받아 합성 출처, R50/Jf120.396/띠장M9.479와 원문명·원문 sourceInput 미포함을 확인했습니다. HTML은 브라우저에서 렌더해 수식·단위·표를 확인했습니다.
- 성능 관찰: macOS Codex IAB, 일반 네트워크·CPU/망 제한 없음. 첫 탭 생성부터 개요 관찰3.35초, 안내 첫 클릭→모달421ms, 지반 진입→canvas와 캡처795ms. 도구 통신을 포함한 관찰 시간이며 FCP/LCP나 통계 벤치마크가 아닙니다. 조작을 막는 긴 정지는 관찰하지 못했습니다.

## 로컬 실제 자료 검산

실제 원본은 공개 저장소에 포함하지 않았습니다. 제출 패키지의 `local-private/ground`에 사용자 검토용 파생자료만 보관합니다.

- NH 14공·41구간을 선정 원문 페이지와 대조했습니다. NH-4 총심도/층 하단의 원문 불일치를 보존하고 미확인 경계를 종료심도로 대체하지 않았습니다.
- X/Y를 E/N으로 정규화했습니다. 주상도 CRS는 확정하지 못했으므로 드론과의 결과는 좌표 기반 개략 중첩입니다. 영상 독립 검사점은 없습니다.
- 실제 ordinary kriging 2,805점의 NumPy와 JS 최대차1.97766e-11로 허용1e-8 통과. 실제 자료 LOO는 지표2.3962m·풍화토 하단6.1835m이며 정확도 보장이 아닙니다.
- CAD 맞춤 잔차는 동일 CAD의 좌표 일관성 검사입니다. 현장 측량 정확도나 침하량으로 사용하지 않습니다.

## 범위와 남은 사람의 작업

선정 네 부재의 후처리 검토이며 외부 탄소성해석·전체 흙막이 안정성을 수행하지 않습니다. 기준 최신판과 본문 적합성은 자동 인증하지 않습니다. 로컬 보관 기록은 브라우저·도메인마다 독립이며 통합 JSON에는 저장한 결과/개정만 포함됩니다.

발표 영상 공개, 실행 종료 후 세션 로그 사본 준비·업로드, 행사 대표자 최종 제출은 사람이 수행합니다. 공개 코드와 데모 배포 검증은 별도로 완료 후 배포 기록에 남깁니다.
